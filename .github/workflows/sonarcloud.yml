name: Build and analyze (SonarCloud)

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  build:
    name: Build and analyze
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Important for blame and coverage

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Cache SonarCloud packages
        uses: actions/cache@v4
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

<<<<<<< HEAD
      - name: Build and analyze (from common/)
        working-directory: ./common
=======
      - name: Build and analyze
>>>>>>> 2fe1ffa12d2d201c199ad6a673e57f1d6bf96587
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar \
<<<<<<< HEAD
            -Dsonar.projectKey=group2-SCM-Fall25 \
            -Dsonar.organization=amirchappalwala
=======
          -Dsonar.projectKey=group2-SCM-Fall25 \
          -Dsonar.organization=amirchappalwala
>>>>>>> 2fe1ffa12d2d201c199ad6a673e57f1d6bf96587
